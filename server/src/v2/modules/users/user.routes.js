const { Router } = require('express');
const userController = require('./user.controller');

const router = Router();

// Define v2 user routes here
// router.get('/', userController.getUsers);

module.exports = router;
